import { DocumentListComponent } from './document-list.component';

describe('DocumentListComponent', () => {
  let component: DocumentListComponent;

  beforeEach(() => {
    component = new DocumentListComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
